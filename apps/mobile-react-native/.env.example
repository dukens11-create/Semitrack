# Set these in your shell or build environment; this file is documentation.
SEMITRAX_API_URL=
MAPBOX_PUBLIC_TOKEN=
# MAPBOX_DOWNLOADS_TOKEN belongs only in native dependency tooling if required.
# Never put Trimble, database, JWT, OAuth, signing or private Mapbox secrets in JS.


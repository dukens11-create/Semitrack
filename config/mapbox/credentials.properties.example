# Public runtime token used by Flutter for maps and destination search.
# Use a Mapbox token beginning with pk. Never put the secret sk. downloads
# token in this file because Dart defines are bundled into the application.
MAPBOX_ACCESS_TOKEN=YOUR_PUBLIC_PK_TOKEN
